services:
  reknotes:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - reknotes-data:/data
    environment:
      - DB_PATH=/data/reknotes.sqlite
    restart: unless-stopped

volumes:
  reknotes-data:
