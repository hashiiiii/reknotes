name: Deploy

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch or tag to deploy (e.g. v1.1.0, main)"
        required: true
      environment:
        description: "Target environment"
        required: true
        type: choice
        options:
          - production
          - development

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ghcr.io/hashiiiii/reknotes:${{ inputs.ref }}
            ghcr.io/hashiiiii/reknotes:${{ inputs.environment }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
